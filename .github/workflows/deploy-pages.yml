name: Deploy React app to GitHub Pages

on:
  push:
    branches:
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  NODE_OPTIONS: --openssl-legacy-provider
  CI: false

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    name: Build React app
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: react-snake/package-lock.json

      - name: Install dependencies
        working-directory: react-snake
        run: npm install

      - name: Build static app
        working-directory: react-snake
        run: npm run build

      - name: Add SPA fallback
        run: cp react-snake/build/index.html react-snake/build/404.html

      - name: Verify build output
        run: |
          test -f react-snake/build/index.html
          test -f react-snake/build/404.html
          ls -la react-snake/build

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: react-snake/build

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy artifact
        id: deployment
        uses: actions/deploy-pages@v4
